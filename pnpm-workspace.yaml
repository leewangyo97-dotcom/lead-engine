# pnpm 11 reads build-script approval from here, not package.json.
# esbuild (via vitest and drizzle-kit) ships a prebuilt binary that its install
# script unpacks. Without this, `pnpm install` exits 1 on
# ERR_PNPM_IGNORED_BUILDS and vitest cannot run.
allowBuilds:
  esbuild: true
